version: '3:8'

services:
  db:
    restart: always
    image: postgres
    container_name: weather_pipeline

    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD : root123
      POSTGRES_DB : weather_data

    ports:
      - "5433:5432"


    volumes:
      - "C:/Users/00001/OneDrive/Documentos/volumes/weatherdata:/var/lib/postgresql"